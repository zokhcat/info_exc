# Usage
```sh
uv run main.py extract <url> -f <format>
```

format flags:
- json(default)
- yaml
- table

fields extracted:
- url
- title
- logo_url
- fonts
- meta - description, keywords, og_title, og_description, og_image