[project]
name = "info-exc"
version = "0.1.0"
description = "Web Scraper"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "playwright>=1.57.0",
    "typer>=0.21.1",
]

[project.scripts]
info-exc = "info_exc.main:main"
